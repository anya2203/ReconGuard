"""ReconGuard reproducible evaluation harnesses.

This package holds standalone, runnable benchmark scripts (as opposed to
`app/evaluation` and `app/investigator/evaluator.py`, which are the in-app
scoring libraries these scripts call into). Historical run artifacts live in
`evaluation/results/`.
"""
